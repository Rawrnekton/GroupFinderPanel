package client.model.streamclients;

/*
 * ClientBase to send the currently loaded Profile to the Server
 * but only after it changed (it will only be constructed in another class when that happens)
 */
public class ClientTest {


}